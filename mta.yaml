_schema-version: 3.3.0
ID: tum-sample
version: 1.0.0
description: "A simple CAP project."
parameters:
  enable-parallel-deployments: true
  deploy_mode: html5-repo
build-parameters:
  before-all:
    - builder: custom
      commands:
        - npm ci
        - npx cds build --production
modules:
  - name: tum-sample-srv
    type: nodejs
    path: gen/srv
    parameters:
      instances: 1
      buildpack: nodejs_buildpack
    build-parameters:
      builder: npm-ci
    provides:
      - name: srv-api # required by consumers of CAP services (e.g. approuter)
        properties:
          srv-url: ${default-url}
    requires:
      - name: tum-sample-destination
      - name: tum-sample-auth
      - name: tum-sample-db

  - name: tum-sample-app-deployer
    type: com.sap.application.content
    path: gen
    requires:
      - name: tum-sample-auth
      - name: srv-api
      - name: tum-sample-html5-repo-host
        parameters:
          content-target: true
          config:
            HTML5Runtime_enabled: true
    build-parameters:
      build-result: app/
      requires:
        - name: tumsamplemodulecatalogue
          artifacts:
            - module-catalogue.zip
          target-path: app/
        - name: tumsamplestudentsmanagement
          artifacts:
            - students-management.zip
          target-path: app/
        - name: tumsamplestudiesmanagement
          artifacts:
            - studies-management.zip
          target-path: app/
    parameters:
      config:
        HTML5Runtime_enabled: true
        init_data:
          instance:
            existing_destinations_policy: update
            destinations:
              - Name: srv-api
                URL: ~{srv-api/srv-url}
                Authentication: NoAuthentication
                Type: HTTP
                ProxyType: Internet
                HTML5.ForwardAuthToken: true
                HTML5.DynamicDestination: true
              - Name: ui5
                URL: https://ui5.sap.com
                Authentication: NoAuthentication
                Type: HTTP
                ProxyType: Internet

  - name: tumsamplemodulecatalogue
    type: html5
    path: app/module-catalogue
    build-parameters:
      build-result: dist
      builder: custom
      commands:
        - npm ci
        - npm run build
      supported-platforms:
        []
  - name: tumsamplestudentsmanagement
    type: html5
    path: app/students-management
    build-parameters:
      build-result: dist
      builder: custom
      commands:
        - npm ci
        - npm run build
      supported-platforms:
        []
  - name: tumsamplestudiesmanagement
    type: html5
    path: app/studies-management
    build-parameters:
      build-result: dist
      builder: custom
      commands:
        - npm ci
        - npm run build
      supported-platforms:
        []

  - name: tum-sample-destinations
    type: com.sap.application.content
    requires:
      - name: tum-sample-auth
        parameters:
          service-key:
            name: tum-sample-auth-key
      - name: tum-sample-html5-repo-host
        parameters:
          service-key:
            name: tum-sample-html5-repo-host-key
      - name: srv-api
      - name: tum-sample-destination
        parameters:
          content-target: true
    build-parameters:
      no-source: true
    parameters:
      content:
        instance:
          existing_destinations_policy: update
          destinations:
            - Name: tum-sample-html5-repository
              ServiceInstanceName: tum-sample-html5-repo-host
              ServiceKeyName: tum-sample-html5-repo-host-key
              sap.cloud.service: tumsample.service
            - Name: tum-sample-auth
              Authentication: OAuth2UserTokenExchange
              ServiceInstanceName: tum-sample-auth
              ServiceKeyName: tum-sample-auth-key
              sap.cloud.service: tumsample.service

  - name: tum-sample-db-deployer
    type: hdb
    path: gen/db
    parameters:
      buildpack: nodejs_buildpack
    requires:
      - name: tum-sample-db

resources:
  - name: tum-sample-destination
    type: org.cloudfoundry.managed-service
    parameters:
      service: destination
      service-plan: lite
      config:
        HTML5Runtime_enabled: true
        init_data:
          instance:
            existing_destinations_policy: update
            destinations:
              - Name: srv-api
                URL: ~{srv-api/srv-url}
                Authentication: NoAuthentication
                Type: HTTP
                ProxyType: Internet
                HTML5.ForwardAuthToken: true
                HTML5.DynamicDestination: true
    requires:
      - name: srv-api
        group: destinations
        properties:
          name: srv-api # must be used in xs-app.json as well
          url: ~{srv-url}
          forwardAuthToken: true

  - name: tum-sample-html5-repo-host
    type: org.cloudfoundry.managed-service
    parameters:
      service: html5-apps-repo
      service-plan: app-host
  - name: tum-sample-auth
    type: org.cloudfoundry.managed-service
    parameters:
      service: xsuaa
      service-plan: application
      path: ./xs-security.json
      config:
        xsappname: tum-sample-${org}-${space}
        tenant-mode: dedicated
        role-collections:
          - name: 'Student (tum-sample ${org}-${space})'
            description: 'generated'
            role-template-references:
              - '$XSAPPNAME.Student'
  - name: tum-sample-db
    type: com.sap.xs.hdi-container
    parameters:
      service: hana
      service-plan: hdi-shared
  - name: tum-sample-html5-runtime
    type: org.cloudfoundry.managed-service
    parameters:
      service: html5-apps-repo
      service-plan: app-runtime
